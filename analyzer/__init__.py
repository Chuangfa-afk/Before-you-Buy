"""
Analyzer package for Before You Buy.

Each module exposes a top-level `analyze_*` function that takes primitive
inputs (strings, numbers, dicts) and returns JSON-serializable structures
for the API layer and frontend to consume.
"""

